<%= form_with(model: test_config) do |form| %>
  <% if test_config.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(test_config.errors.count, "error") %> prohibited this test_config from being saved:</h2>

      <ul>
        <% test_config.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :short_name %>
    <%= form.text_field :short_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
